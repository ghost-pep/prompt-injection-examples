we use digital signatures to verify the integrity of our driver packages.
All drivers signed by the Hardware Dev Center are SHA2 signed. 
Kernel-mode driver binaries are embedded signed with dual (SHA1 and SHA2) certificates from a third-party certificate vendor.
